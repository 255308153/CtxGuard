<!-- CTXGUARD_AUTO_RULES:START -->
## CtxGuard Auto-Learned Rules & Loop Guards
> *Last updated: 2026-09-17 22:09:44 (Generated automatically from incident analysis)*

### 1. Command Patterns (`Calling tool `bash``)
- **Rule**: When `bash` fails repeatedly, stop and inspect argument format or prerequisites rather than repeating identical parameters.
- **Context & Rationale**: Observed 89 consecutive execution errors with `bash`.

### 2. Command Patterns (`Calling tool `ctx_expand``)
- **Rule**: When `ctx_expand` fails repeatedly, stop and inspect argument format or prerequisites rather than repeating identical parameters.
- **Context & Rationale**: Observed 4 consecutive execution errors with `ctx_expand`.

### 3. Command Patterns (`Calling tool `edit``)
- **Rule**: When `edit` fails repeatedly, stop and inspect argument format or prerequisites rather than repeating identical parameters.
- **Context & Rationale**: Observed 12 consecutive execution errors with `edit`.

### 4. Command Patterns (`Calling tool `read``)
- **Rule**: When `read` fails repeatedly, stop and inspect argument format or prerequisites rather than repeating identical parameters.
- **Context & Rationale**: Observed 21 consecutive execution errors with `read`.

### 5. Command Patterns (`Calling tool `write``)
- **Rule**: When `write` fails repeatedly, stop and inspect argument format or prerequisites rather than repeating identical parameters.
- **Context & Rationale**: Observed 22 consecutive execution errors with `write`.

<!-- CTXGUARD_AUTO_RULES:END -->
